auto-install-peers=true
strict-peer-dependencies=false
# esbuild/workerd/lmdb ship prebuilt binaries via postinstall; Angular's builder
# and wrangler need them. Approved explicitly rather than blanket-enabled.
enable-pre-post-scripts=true
