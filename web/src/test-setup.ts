/**
 * Vitest setup for the Angular unit suite. Kept minimal on purpose: the pieces
 * worth testing here — the reducer, the command catalogue, the chart options —
 * are pure, so most of the suite needs no TestBed at all.
 */
export {};
